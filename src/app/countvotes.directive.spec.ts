import { CountvotesDirective } from './countvotes.directive';

describe('CountvotesDirective', () => {
  it('should create an instance', () => {
    const directive = new CountvotesDirective();
    expect(directive).toBeTruthy();
  });
});
